import { MainHeader, MainTitle } from "./Header.styled";

export const Header = () => {
   return (
      <MainHeader>
         <MainTitle>Your Simple ToDo App</MainTitle>
      </MainHeader>
   );
};
